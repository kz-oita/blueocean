= render 'layouts/header'
= render 'layouts/main_menu'
.contents-title
  = "『#{@tag.name}』の投稿"
.contents-wrap
  .posts-wrap
    - @posts.each do |post|
      = render partial: "posts/post", locals: { post: post }