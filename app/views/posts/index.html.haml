= render 'layouts/header'
.title-wrap
  %p.main-title
    Blue Ocean
.contents-wrap
  .contents-title--main
    最新の投稿
  .posts-wrap
    - @posts.each do |post|
      = render partial: "post", locals: { post: post }
  .paginate
    = paginate(@posts)
.footer