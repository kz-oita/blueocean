= render 'layouts/header'
.title-wrap
  %p.main-title
    Blue Ocean
.contents-wrap
  .posts
    - @posts.each do |post|
      = render partial: "post", locals: { post: post }

= paginate(@posts)

.footer