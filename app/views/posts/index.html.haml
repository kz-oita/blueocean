= render 'layouts/header'
.title-wrap
  %p.main-title
    Blue Ocean
.contents-wrap
  .contents-title
    みんなの写真
  .posts-wrap
    - @posts.each do |post|
      = render partial: "post", locals: { post: post }

= paginate(@posts)

.footer