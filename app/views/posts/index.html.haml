= render 'layouts/header'
.title-wrap
  %h1.main-title
    Blue Ocean
= render 'layouts/recommend'
.contents-wrap
  .contents-title--post
    Posts
  .posts-wrap
    - @post.each do |post|
      = render partial: "post", locals: { post: post }
    -# .paginate
    -#   = paginate(@post)
  -# .all-btn
  -#   %a.contents-all-btn{:href => "/alllists"}
  -#     = icon('fa', 'hand-point-right')
  -#     All Posts

  .contents-title--user
    Users
  .users-wrap
    - @user.each do |user|
      = render partial: "user", locals: { user: user }
  -# .all-btn
  -#   %a.contents-all-btn{:href => "/alllists"}
  -#     = icon('fa', 'hand-point-right')
  -#     All Users

  .contents-title--tag
    Tags
  .tags-wrap
    - @tag.each do |tag|
      -# = link_to "##{tag.name}", "#", class: "tag-name"
      = link_to "##{tag.name}", tag_path(tag.name), class: "tag-name"
  -# .all-btn
  -#   %a.contents-all-btn{:href => "/alllists"}
  -#     = icon('fa', 'hand-point-right')
  -#     All Tags
= render 'layouts/footer'