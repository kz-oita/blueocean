.title-wrapper
  %p.title-message
    ダイビング特化型コミュニティサイト
  %p.main-title
    Blue Ocean
  -# .title-wrapper__contents
  -#   %p.first.message
  -#     ダイビングQ&A
  -#   %p.second.message
  -#     ダイビング情報
  
.contents-wrapper
  .posts
    - @posts.each do |post|
      = render partial: "post", locals: { post: post }

= paginate(@posts)

.footer